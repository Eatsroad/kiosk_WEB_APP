import React from 'react';
import StoreInfoPresenter from './StoreInfoPresenter';

const StoreInfoContainer = (): JSX.Element => {
  return (<StoreInfoPresenter/>);
};

export default StoreInfoContainer;